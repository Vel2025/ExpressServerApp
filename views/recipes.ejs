<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Recipes</title>
  <link rel="stylesheet" href="../public/styles.css">
</head>
<body>
  <h1>Recipes</h1>
  
  <ul>
    <% recipes.forEach(recipe => { %>
      <li>
        <h3><%= recipe.title %></h3>
        <p>Category: <%= categories.find(c => c.id === recipe.categoryId)?.name %></p>
        <p>Ingredients: <%= recipe.ingredients.join(', ') %></p>
        <p>Instructions: <%= recipe.instructions %></p>
      </li>
    <% }) %>
  </ul>
  <a href="/">Back to Home</a>



</body>
</html>